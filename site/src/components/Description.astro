---
// site/src/components/Description.astro
// Shared one-line description style used across recent lists.
interface Props { text?: string; }
const { text } = Astro.props;
---
{text && (
  <p class="mt-1 text-[16px] leading-snug text-muted">{text}</p>
)}
